# Pages Folder

GuideAtlas frontend uses the Next.js App Router in `site/app/`.

This folder is intentionally kept as a compatibility placeholder for teams/tools
that expect a classic `pages/` layout in monorepos.
